tableextension 50502 "WDC-QA Item Ledger Entry" extends "Item Ledger Entry"
{
    fields
    {

        // field(50502; "Packaging Consolidation No."; Code[20])
        // {
        //     CaptionML = ENU = 'Packaging Consolidation No.', FRA = 'N° regroupement emballage';
        //     DataClassification = ToBeClassified;
        //     TableRelation = IF ("Entry Type" = CONST(Sale)) Customer."No." ELSE IF ("Entry Type" = CONST(Purchase)) Vendor."No.";
        // }
    }
}
